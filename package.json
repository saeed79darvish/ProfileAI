{
  "dependencies": {
    "nodemailer": "^7.0.12",
    "sharp": "^0.34.5",
    "styled-components": "^6.1.19"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  },
  "scripts": {
    "build": "cd frontend && rm -rf build && npm run build:no-typecheck",
    "deploy": "npm run build && wrangler deploy",
    "deploy:nobuild": "wrangler deploy",
    "preview": "npm run build && wrangler dev"
  }
}
